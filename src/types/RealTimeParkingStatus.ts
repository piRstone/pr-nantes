export enum RealTimeParkingStatus {
  's0' = 'invalid', // Display no message
  's1' = 'closed', // Displays 'FERME'
  's2' = 'subscribers', // Displays 'ABONNES'
  's5' = 'open', // Displays the count of spots or 'COMPLET'
}
